const IP = 'http://10.58.7.207:8000';

const API = {
  default: `${IP}`,
  login: `${IP}/login`,
  author: `${IP}/products/authors`,
  main: `${IP}/products/main`,
  detail: `${IP}/products/1`,
};

export default API;
